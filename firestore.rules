rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow reads and writes to quizAnswers collection
    match /quizAnswers/{document=**} {
      allow read, write: if true;
    }
  }
}
